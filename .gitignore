node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# git worktrees live inside the repo root
.worktrees/
.claude/worktrees/

# env / secrets — never commit
.env
.env.*
!.env.example

# editor / OS
.DS_Store
.idea/
.vscode/

# drizzle generated artifacts kept; local pg data not
pgdata/

# playwright artifacts
apps/web/test-results/
apps/web/playwright-report/

# F-17 experiment research data (per-subject CSV downloads — privacy). Keep the
# directory (.gitkeep) but never commit subject data.
experiments/baseline/results/*.csv
